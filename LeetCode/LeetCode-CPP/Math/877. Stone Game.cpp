#include <iostream>

using namespace std;

// 先手的可以选则拿取index为奇数或者偶数的堆，
// 总数为奇数，所以必然有一个大，所以先手必然胜利。

class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        return true;
    }
};

int main() {

    return 0;
}
